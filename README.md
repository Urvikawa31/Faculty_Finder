# Faculty_Finder
Built an end-to-end data engineering pipeline to crawl university faculty directories, extract profile data, store raw HTML in SQLite, and serve it via FastAPI for downstream NLP and semantic search.
